# Quickshell/QML製 右下扇形ランチャー 要件定義（Codex引き渡し用）

## 1. 文書の目的

NixOS + Niri（Wayland）環境で使用する、新しいアプリケーションランチャー兼汎用選択メニューを、Quickshellを実行基盤としたQMLアプリとして実装する。

本アプリは、一般的な中央配置の検索ランチャーではなく、画面右下を基点として扇形に項目を表示する。アプリケーション起動だけでなく、ユーザー定義の階層メニュー、外部スクリプトが生成した候補、画像ファイルのサムネイルなどを同じUIで扱う。

最初の実用目標は、既存の壁紙選択スクリプト `select-wallpaper.sh` で使用している `wofi --dmenu` を置き換えることである。壁紙適用処理そのものは既存スクリプトに残し、本アプリは候補の表示と選択結果の返却を担当する。

UI本体の実装基盤はQuickshell + QMLに決定する。外部スクリプトとの標準入出力を橋渡しする薄いCLIラッパーを別途用意する。プロジェクト名は「要（kaname
）」、本書でCLI実行ファイル名を `kaname` と記載する。

---

## 2. プロダクトの基本方針

### 2.1 役割

本アプリは、以下を統合した汎用メニュー基盤とする。

- XDGデスクトップエントリを読み込むアプリケーションランチャー
- ユーザーが設定ファイルで定義できる階層型コマンドメニュー
- `wofi --dmenu` / `dmenu` に近い標準入出力型の選択UI
- 画像サムネイルを扱えるファイル選択UI
- 外部スクリプトから動的に候補を受け取るメニューフロントエンド
- matugenの生成色を再起動なしで反映する動的テーマUI

### 2.2 最重要の設計原則

- デザイン性と滑らかなアニメーションを重視する。
- 基本配置は画面右下の扇形とする。
- マウスとキーボードの両方で操作できるようにする。
- 項目数が多い場合も、扇形という外観を崩さず円形スクロールで閲覧できるようにする。
- アプリ、コマンド、サブメニュー、画像候補などを共通の「項目」モデルとして扱う。
- 外部スクリプトとの連携ではUnix的な標準入力・標準出力を優先する。
- UIとアニメーションはQuickshell/QMLで実装する。
- 背景や各UI部品の透明度を色とは独立して変更可能にする。
- matugenが生成したパレットを監視し、壁紙変更後に自動反映する。
- MVPでは既存の壁紙変更ロジックを再実装しない。候補表示部分だけを置き換える。

---

## 3. 対象環境

### 3.1 必須対象

- Linux
- Wayland
- Niriコンポジタ
- NixOS
- Quickshell
- Qt 6 / Qt Quick / QML
- matugen
- WebP表示に必要なQt Image Formats

### 3.2 将来対象

- 他のwlroots系Waylandコンポジタ
- GNOME / KDEなど、Niri以外のWayland環境

MVPではNiri上で正しく動作することを優先し、X11対応は必須としない。

### 3.3 実行アーキテクチャ

アプリは次の二つを基本単位とする。

1. **Quickshell/QML UI本体**
   - ユーザーセッション中は常駐する。
   - 通常時はウィンドウを非表示にする。
   - IPC要求を受けたときだけ右下の扇形UIを表示する。
   - 描画、アニメーション、検索、項目選択、画像表示、テーマ反映を担当する。

2. **CLIブリッジ `radial-launcher`**
   - `wofi --dmenu`に近い標準入力・標準出力インターフェースを提供する。
   - stdinから受け取った候補を、一時リクエストファイルまたは安全なIPC転送形式でQML本体へ渡す。
   - 選択完了まで待機し、選択された元の値だけをstdoutへ返す。
   - ログはstderrへ出す。

Quickshellの`IpcHandler`を呼び出し入口として利用する。大量の候補そのものをIPCのコマンドライン引数へ詰め込まず、リクエストIDと候補ファイルのパスなど、少数の制御情報だけをIPCで渡す構成を基本とする。

選択結果の返却方法は、利用するQuickshellバージョンの機能を確認したうえで、IPCシグナル、リクエスト単位のFIFO、Unixソケット、結果ファイルなどから安全で競合しない方式を選ぶ。同時リクエストを誤配しないよう、各要求に一意なリクエストIDを持たせる。

Quickshellは1.0未満で仕様変更の可能性があるため、Nix flakeまたはNixOS設定で使用バージョンを固定する。

### 3.4 推奨プロジェクト構成

本プロジェクトは単なる一枚の個人用`shell.qml`ではなく、独立して起動・設定・配布できるQMLアプリとして整理する。

概念例：

```text
radial-launcher/
├── flake.nix
├── README.md
├── bin/
│   └── radial-launcher
├── quickshell/
│   ├── shell.qml
│   ├── Launcher.qml
│   ├── LauncherWindow.qml
│   ├── LauncherState.qml
│   ├── model/
│   │   ├── LauncherItem.qml
│   │   ├── DmenuSource.qml
│   │   ├── DesktopEntrySource.qml
│   │   └── ConfigSource.qml
│   ├── ui/
│   │   ├── FanLayout.qml
│   │   ├── FanItem.qml
│   │   ├── ImageItem.qml
│   │   ├── CenterText.qml
│   │   └── SearchField.qml
│   ├── theme/
│   │   ├── Theme.qml
│   │   └── FallbackTheme.qml
│   └── ipc/
│       └── LauncherIpc.qml
├── config/
│   ├── default.json
│   └── menus.json
└── matugen/
    └── radial-launcher-colors.json
```

実際の分割は実装時に調整してよいが、描画、状態管理、入力ソース、IPC、テーマを一つの巨大QMLファイルへ混在させない。

---

## 4. UIコンセプト

### 4.1 基本レイアウト

- ランチャーは画面右下を基点として表示する。
- 項目は右下に中心を持つ仮想的な円の円周上へ、扇形に配置する。
- 仮想円の中心は画面右下ぴったり、または画面外へオフセットできるようにする。
- 画面には円全体を表示せず、巨大な円形機構の一部が画面内へ現れるような外観を目指す。
- 背景は透明または半透明とし、元のデスクトップが見えるようにする。
- デフォルトのアンカーは右下とするが、将来的には中央円、画面端の半円、他の四隅にも対応可能な座標設計とする。

### 4.2 座標モデル

UI項目の配置は、直交座標へ個別に固定せず、中心点・半径・角度による極座標を基本とする。

少なくとも以下を設定可能にする。

- アンカー位置
- 仮想円の中心オフセット
- 半径
- 扇の開始角度
- 扇の終了角度、または表示角度
- 同時に表示する項目数
- 項目間隔
- 項目サイズ

これにより、同じ描画エンジンから以下のレイアウトを将来生成できるようにする。

- 中央の360度円形メニュー
- 画面端の180度半円メニュー
- 四隅の90度前後の扇形メニュー
- 中心が画面外にある浅い円弧メニュー

ただし、MVPで実装・調整する既定レイアウトは右下扇形のみでよい。

### 4.3 項目表示

通常項目は、少なくとも以下を表示できる。

- アイコン
- 表示名
- 任意の説明文
- 割り当てられたキーバインド
- 選択状態
- 無効状態

画像項目は、アイコンではなく画像サムネイルを表示できる。

選択中の項目は、次のいずれか、または組み合わせによって明確に示す。

- 拡大
- 中心方向または外周方向への移動
- 発光、枠線、色変化
- 非選択項目の縮小・減光
- 中央テキスト領域への名前・説明表示

### 4.4 中央テキスト領域

仮想円の中心付近から画面内側へずらした位置に、テキスト領域を設ける。

通常時には以下を表示する。

- 現在のメニュー名またはプロンプト
- 選択中の項目名
- 任意の説明
- 現在位置（例：`3 / 17`）

検索モードでは、この領域を検索入力欄として使用する。

右下配置のため、文字列は主に左方向または左上方向へ伸びるレイアウトとし、画面外へ切れないようにする。

### 4.5 背景と透明度

- Quickshellのウィンドウ背景は透過可能にする。
- 不透明な矩形パネルで画面右下を覆うことを必須とせず、円弧、項目、中央テキストなど必要な要素だけを浮かせられるようにする。
- 半透明背景を使用する場合も、背景色と透明度を別々に設定する。
- 透明度は少なくとも次の単位で変更できるようにする。
  - UI全体のマスター透明度
  - 背景面
  - 画面全体の暗転オーバーレイ
  - 通常項目
  - 非選択項目
  - 円弧・目盛り・ガイド線
  - 補助テキスト
  - 画像サムネイル
- 検索モード中だけ背景を濃くするなど、状態に応じた透明度変更を可能にする。
- 透明度変更は設定再起動を必要とせず、可能な限り実行中に反映する。
- 背景ぼかしは任意の将来拡張とし、MVP必須にはしない。

---

## 5. 円形スクロール

### 5.1 基本動作

- 一度に表示可能な数を超えた項目は、円周上に続いて存在するものとして扱う。
- マウスホイールまたはキーボード操作で仮想円を回転させ、画面外から次の項目を入れる。
- 項目は突然ページ切り替えせず、円弧に沿って連続的に移動する。
- 画面外へ消える項目はフェードアウトし、新しく入る項目はフェードインしてよい。
- 見た目には慣性を感じさせてもよいが、最終的な選択位置は必ず項目単位でスナップする。
- 高速入力中も選択位置を見失わず、入力の取りこぼしを起こさないこと。

### 5.2 位置表示

大量項目で現在位置を見失わないよう、以下のいずれかを表示する。

- `現在位置 / 総件数`
- 外周の細い弧によるスクロール位置表示
- その両方

一般的な直線スクロールバーをそのまま置くのではなく、円形デザインに馴染む表示を優先する。

---

## 6. 操作仕様

### 6.1 キーボード操作

既定では少なくとも以下を提供する。

| 入力 | 動作 |
|---|---|
| 矢印キー | 選択項目の移動 |
| Enter | 項目の実行、決定、またはサブメニューへ入る |
| Escape | 一階層戻る。ルートまたは検索解除後はランチャーを閉じる |
| Backspace | 検索文字の削除。通常モードでは一階層戻る設定も可能 |
| `/` | 検索モードへ入る |

次項目、前項目、回転、決定、戻る、検索などのキーは設定ファイルで変更可能にする。

### 6.2 項目・階層固有のキーバインド

- 各項目に任意のキーバインドを定義できるようにする。
- キーバインドは現在の階層内だけで有効なローカルバインドを基本とする。
- 同じキーを異なる階層で再利用できるようにする。
- 項目の付近に割り当てキーを表示できるようにする。
- 設定により、キー入力直後に実行する方式と、選択だけを移動してEnterで確定する方式を選べるようにする。
- 電源オフなど危険性のある操作には確認を設定できる設計が望ましい。

### 6.3 マウス操作

- 項目のクリックで選択・実行またはサブメニュー遷移を行う。
- マウスホイールで円形スクロールを行う。
- 中央テキスト領域をクリックすると検索モードへ入る。
- 項目へのホバーで選択状態を移動できるようにするかは設定可能とする。
- Kandoのようなマウス軌跡による高速選択は将来拡張とし、MVP必須にはしない。

### 6.4 閉じる条件

- Escapeで閉じる。
- 項目を実行したら、設定上残す指定がない限り閉じる。
- 背景領域クリックで閉じられるようにする。
- フォーカス喪失時に閉じるかは設定可能とする。

---

## 7. 検索モード

### 7.1 起動

以下のどちらかで検索モードへ入る。

- `/`キーを押す
- 中央テキスト領域をクリックする

`/`はモード切り替えに使用し、検索文字列自体には含めない。

### 7.2 表示

- 中央テキスト領域を検索入力欄へ変形させる。
- 通常項目から検索結果への変化は、可能な限り連続したアニメーションで表現する。
- 検索結果も基本的に右下の扇形・円形スクロールで表示する。
- 文字列が長い場合も、入力欄が画面外へ出ないこと。

### 7.3 検索対象

少なくとも以下を対象にする。

- 表示名
- 説明
- デスクトップエントリ名
- ファイル名
- ファイルパス
- 任意の検索キーワード

デフォルトは現在のメニューとその配下を優先しつつ、全体検索も可能な設計とする。検索範囲は設定で次から選べることが望ましい。

- 現在階層のみ
- 現在階層以下
- 全登録項目

### 7.4 解除

- Escapeで検索を解除し、検索開始前のメニュー状態へ戻る。
- 検索文字列が空の状態でBackspaceを押した場合も、検索解除としてよい。
- 検索前の階層とスクロール位置を復元する。

---

## 8. 項目データモデル

UI側では、すべての表示対象を共通の項目モデルとして扱う。

項目は少なくとも以下の情報を持てるようにする。

| フィールド | 内容 |
|---|---|
| `id` | 一意な識別子 |
| `type` | 項目の種類 |
| `label` | 表示名 |
| `description` | 補足説明 |
| `icon` | アイコンテーマ名またはアイコンパス |
| `image` | サムネイル画像パス |
| `value` | dmenuモードなどで返す値 |
| `keywords` | 検索専用キーワード |
| `key` | 階層内キーバインド |
| `disabled` | 選択不能状態 |
| `children` | 子項目 |
| `action` | 実行内容 |

想定する項目種別は以下。

- デスクトップエントリ
- コマンド
- サブメニュー
- 外部コマンドによる動的サブメニュー
- ファイル
- URL
- 画像候補
- 組み込みアクション

MVPではすべてを実装する必要はないが、内部モデルは種類の追加を妨げない構造にする。

---

## 9. デスクトップエントリモード

### 9.1 読み込み

- Quickshellの`DesktopEntries`および`DesktopEntry`を第一候補として利用し、XDGデスクトップエントリを取得する。
- ユーザー領域とシステム領域の両方を対象にする。
- `Hidden`、`NoDisplay`などを適切に扱う。
- 表示名、アイコン、実行コマンド、カテゴリ、検索キーワードを読み込む。
- アイコンテーマからアイコンを解決する。
- デスクトップエントリの実行フィールドを安全に処理し、単純なシェル文字列連結で実行しない。
- Quickshell標準の`DesktopEntry.execute()`で扱えない`runInTerminal`やフィールドコードがある場合、MVPでは制限として明記し、後から安全な実行ヘルパーを追加できる構造にする。

### 9.2 表示と検索

- すべてのアプリを右下扇形へ表示し、表示しきれない分は円形スクロールする。
- ユーザー定義のサブメニュー内へ、特定のデスクトップエントリを配置できる。
- デスクトップエントリ一覧そのものを動的なサブメニューとして配置できる設計が望ましい。
- アプリ名の入力による検索を提供する。

---

## 10. ユーザー定義階層

### 10.1 設定可能な内容

ユーザーは設定ファイルで以下を定義できる。

- ルートメニュー
- 任意の深さのサブメニュー
- 各項目の表示名、アイコン、説明
- 各項目のキーバインド
- デスクトップエントリの参照
- コマンド実行
- ファイル・URLを開く操作
- 外部スクリプトを候補プロバイダーとして使用する動的メニュー
- メニューごとのレイアウトプロファイル

### 10.2 設定形式

Quickshell/QMLから安全かつ簡単に読め、Nixから生成しやすいJSONを初期設定形式の第一候補とする。QML内へデフォルト値を持たせつつ、ユーザー設定、独自階層、レイアウト値、透明度などを外部JSONで上書きできるようにする。

要件は以下。

- 人間が直接編集しやすい。
- 階層構造が読み取りやすい。
- Nix / Home Managerから生成しやすい。
- 設定エラー時に、ファイル位置と原因を分かりやすく報告する。
- 将来的にGUI設定画面を追加しても、同じ内部モデルを利用できる。
- Quickshellの`FileView`などで変更を監視し、可能な項目はアプリを終了せず再読み込みする。

TOMLまたはKDL対応は将来拡張としてよい。採用する場合も、QMLの描画層へ直接パーサー処理を散在させず、共通の内部モデルへ変換する。

### 10.3 コマンド実行の安全性

- 通常のコマンドは、可能な限り実行ファイルと引数の配列として定義する。
- シェル経由の実行は明示的に指定した場合だけ許可する。
- 標準入力から受け取った文字列を暗黙にシェルコマンドとして実行しない。

---

## 11. dmenu / wofi互換モード

### 11.1 目的

既存のシェルスクリプトから、`wofi --dmenu`と同様に使用できるモードを提供する。

基本契約は次の通り。

```text
標準入力：改行区切りの候補
画面：候補を右下扇形で表示
標準出力：選択された元の一行
```

### 11.2 必須CLI

最低限、次の呼び出しを提供する。

```bash
printf '%s\n' "Image" "Video" |
  radial-launcher --dmenu --prompt "Select Mode"
```

必要なオプションは以下。

- `--dmenu`：標準入出力型の選択モード
- `--prompt TEXT`：中央テキスト領域へ表示するプロンプト
- `--profile NAME`：任意の外観・操作プロファイルを指定（MVP後半でもよい）

wofi固有のCSSや設定ファイルとの互換性は不要。`--conf`、`--style`をそのまま実装する必要はない。

### 11.3 CLIブリッジとIPC

- `radial-launcher`はQML本体そのものではなく、Quickshell常駐プロセスへ要求を渡す薄いCLIブリッジとする。
- CLIはstdinを読み切り、候補リストとプロンプト、入力形式、プロファイル、リクエストIDをQML本体へ渡す。
- 大量の候補は一時ファイルなどを利用し、IPC引数長へ依存しない。
- 一時ファイルは安全な一時ディレクトリ内へ作成し、処理完了またはキャンセル後に片付ける。
- QML本体が未起動の場合は、起動を試みるか、明確なエラーをstderrへ出す。
- 一つの要求処理中に別の要求が来た場合の方針を定義する。MVPでは後着要求を拒否してもよいが、別要求の結果を返してはならない。
- 選択完了通知はリクエストIDと関連付ける。

### 11.4 出力契約

- 項目を選択した場合、入力された元の一行を加工せず標準出力へ返す。
- 末尾に改行を付けてよい。
- 正常選択時の終了コードは`0`。
- キャンセル時は標準出力へ何も出さず、終了コードは`1`。
- 入力形式や設定のエラーは、キャンセルと区別できる非ゼロ終了コードにする。
- ログや警告は標準エラーへ出し、標準出力を汚さない。

### 11.5 `img:`互換入力

現在の壁紙スクリプトとの互換性のため、以下の入力を画像項目として解釈する。

```text
img:/absolute/path/to/image.png
```

動作要件は以下。

- `img:`以降のパスから画像を読み込み、サムネイルとして表示する。
- 検索対象にはファイル名とパスを含める。
- 表示名のデフォルトはファイル名とする。
- 項目を選択した場合、`img:`を含む元の一行をそのまま返す。
- 対応形式は少なくともPNG、JPEG、WebP。
- 読み込みに失敗した場合はプレースホルダーを表示し、アプリ全体を終了させない。

これは完全なwofi構文互換ではなく、本プロジェクトの既存スクリプトで使用中の`img:`形式に対する互換機能とする。

### 11.6 将来の構造化入力

プレーンテキストに加えて、JSON Linesなどによる構造化入力を将来追加する。

例：

```json
{"id":"shrine-night","label":"機械仕掛けの神社","value":"/wallpapers/shrine.webp","image":"/wallpapers/shrine.webp","keywords":["神社","夜","和風","青"]}
```

構造化入力では、表示名、画像、返却値、検索語、キーバインドを分離できるようにする。

構造化入力の選択結果は、CLIオプションに応じて以下から選べる設計が望ましい。

- 元のJSON行
- `value`
- `id`
- JSONオブジェクト

MVPではプレーンテキストと`img:`互換を優先し、JSON Linesは次段階としてよい。

---

## 12. 動的プロバイダー

設定内のサブメニューに、外部コマンドまたはスクリプトを候補生成元として指定できるようにする。

概念例：

```toml
[[menu.items]]
type = "provider"
label = "Wallpapers"
command = ["list-wallpapers"]
input_format = "lines"
```

項目を開いた時点でコマンドを実行し、その標準出力を子項目として表示する。

要件は以下。

- 候補生成中はローディング状態を表示する。
- コマンド失敗時はメニュー内にエラーを表示し、親階層へ戻れるようにする。
- プロバイダーの標準エラーをユーザー向け表示またはログで確認できるようにする。
- 初期実装では出力をEOFまで読んでから表示してよい。
- 項目の追加・更新・削除を逐次反映するストリーミング入力は将来拡張とする。

---

## 13. 画像・サムネイル処理

### 13.1 表示

- 画像項目は扇形の円弧上へサムネイルとして表示する。
- 選択中の画像を他より大きく表示する。
- 前後の候補は小さくし、必要に応じて減光する。
- 選択中画像の名前、位置、任意の説明を中央テキスト領域へ表示する。
- 横長画像を無理に正方形へ変形せず、アスペクト比を維持してクロップまたはレターボックスする。

### 13.2 パフォーマンス

- 元画像を毎回フルサイズで描画しない。
- サムネイル生成または縮小デコードを行う。
- 画像読み込みは可能な限り非同期に行い、UIを停止させない。
- 現在選択中と前後数件を優先して読み込む。
- 画像キャッシュを持てる設計とする。
- キャッシュキーには元ファイルのパス、更新日時、要求サイズなどを利用する。
- キャッシュがなくても段階的に表示し、全画像読み込み完了までメニュー表示を待たせない。

### 13.3 動画

MVPでは動画の直接再生や動画プレビューを行わない。

既存スクリプトがffmpegまたはyt-dlpで生成したPNGサムネイルを、通常の画像項目として表示する。動画プレビューは将来拡張とする。

---

## 14. 現行壁紙スクリプトとの統合

### 14.1 現状

既存の`select-wallpaper.sh`は以下を行っている。

1. `Image`または`Video`をwofiで選択する。
2. 画像の場合、画像ファイルを列挙し、各行を`img:ファイルパス`としてwofiへ渡す。
3. 動画の場合、ローカル動画およびYouTube動画のサムネイルを準備する。
4. サムネイルパスを`img:ファイルパス`としてwofiへ渡す。
5. 選択結果から`img:`を外し、実際の画像、動画、YouTube URLへ復元する。
6. `apply-wallpaper.sh`を呼び出す。

### 14.2 MVPで置き換える範囲

MVPではwofi呼び出しだけを`radial-launcher`へ置き換える。

置き換え前：

```bash
MODE=$(printf '%s\n' "Image" "Video" |
  wofi --dmenu --prompt "Select Mode")
```

置き換え後：

```bash
MODE=$(printf '%s\n' "Image" "Video" |
  radial-launcher --dmenu --prompt "Select Mode")
```

画像・動画候補も同様に、`img:`行を標準入力から渡す。

### 14.3 変更しない範囲

MVPでは以下を新アプリへ移植しない。

- 壁紙ファイルの探索ロジック
- ローカル動画サムネイルのffmpeg生成
- YouTubeサムネイルのyt-dlp取得
- 不要サムネイルの削除
- サムネイル名から動画やYouTube URLを復元する処理
- `apply-wallpaper.sh`
- mpv動画壁紙の制御
- 他UIへのテーマ再読み込み通知

新アプリは選択した元の`img:`行を返し、既存スクリプトがその後を処理する。

### 14.4 matugen連携

壁紙変更処理そのものは既存スクリプトに残すが、壁紙適用フローからmatugenを呼び出し、新ランチャー用のテーマJSONを生成する処理を追加する。

想定フロー：

1. 新ランチャーで壁紙を選択する。
2. CLIブリッジが選択した`img:`行を既存スクリプトへ返す。
3. 既存スクリプトが`apply-wallpaper.sh`を呼ぶ。
4. 壁紙を適用する。
5. matugenを対象壁紙に対して実行する。
6. matugenが新ランチャー用の色JSONをアトミックに更新する。
7. 常駐中のQML本体がファイル変更を検知する。
8. UIを再起動せず、新しい色へアニメーション付きで遷移する。

動画壁紙では、可能であれば既存のサムネイル画像をmatugenの色生成元として使用する。YouTube動画とローカル動画の両方で、`apply-wallpaper.sh`へ渡しているサムネイルパスを利用できる構成が望ましい。

matugenの呼び出し失敗またはテーマJSONの破損によって、壁紙適用やランチャー起動全体を失敗させない。直前の有効なテーマまたは内蔵フォールバックテーマを維持する。

既存のpywal / pywal16処理を直ちに削除することは要件としない。他UIがpywalを利用している間は併存可能とし、新ランチャーの色源はmatugenを正式採用する。

### 14.5 将来統合

将来的には、`Image`と`Video`を新アプリ内の階層メニューとして一回の表示セッションで遷移できるようにしてよい。

その場合も、壁紙適用処理は独立した外部コマンドとして保ち、新アプリと密結合させないことが望ましい。

---

## 15. アニメーション

### 15.1 必須表現

- 起動時、項目が右下または画面外から扇形へ展開する。
- 終了時、項目が右下または画面外へ収納される。
- 選択移動は円弧に沿って滑らかに行う。
- 円形スクロール時、項目が連続的に回転する。
- サブメニュー遷移時、親項目を起点として子項目が展開したように見せる。
- 検索モード移行時、中央テキスト領域が入力欄へ連続的に変形する。

### 15.2 操作応答性

- アニメーション中も入力を受け付ける。
- 高速なキー入力で状態が破綻しない。
- 見た目のアニメーションと論理上の選択位置を分離し、論理状態は即座に更新する。
- `prefers-reduced-motion`相当のアニメーション軽減設定を用意することが望ましい。
- アニメーション速度、イージング、拡大率などを設定可能にする。

---

## 16. Wayland・ウィンドウ動作

- Quickshellの`PanelWindow`を基本として、ランチャーを通常ウィンドウではなくオーバーレイとして表示する。
- `right`と`bottom`へアンカーし、Niri上で右下へ安定して配置する。
- 必要に応じて`WlrLayershell`のレイヤーとキーボードフォーカスを明示する。
- 背景透過に対応する。
- 表示中は必要なキーボード入力を受け取る。
- 他の通常ウィンドウより前面へ表示する。
- Niri上で右下へ安定して配置できること。
- マルチモニターでは、原則として現在フォーカス中またはポインターがある出力へ表示する。
- 表示先モニターは設定またはCLIで上書きできる設計が望ましい。
- 画面サイズ、スケーリング、HiDPIを考慮する。
- UIの透明部分全体で不要にクリックを奪うか、必要部分だけ入力領域にするかを検討し、背景クリックによるキャンセル要件と両立させる。

Phase 0では、Quickshellの`PanelWindow`およびWayland layer-shellで、前面表示、透過、キーボードフォーカス、右下アンカー、複数出力がNiri上で期待どおり動くかを最初に検証する。

---

## 17. 設定プロファイル

用途ごとに外観と操作を切り替えられるプロファイル機能を想定する。

例：

| プロファイル | 用途 | 表示 |
|---|---|---|
| `applications` | アプリ起動 | 小さめのアイコン主体の右下扇形 |
| `wallpaper-image` | 静止画壁紙 | 大きな横長サムネイル主体 |
| `wallpaper-video` | 動画壁紙 | サムネイルと動画種別バッジ |
| `power` | 電源操作 | 項目数の少ない確認重視メニュー |

プロファイルで変更可能な候補：

- アンカー位置
- 半径と表示角度
- 表示件数
- アイコン・サムネイルサイズ
- ラベルの表示方法
- アニメーション
- キーバインド
- 検索範囲
- 項目選択時に即時実行するか
- 配色・装飾

MVPでは一つの右下扇形プロファイルだけでもよいが、値をコードへ散在させず、後からプロファイル化しやすくする。

---

## 18. テーマ

### 18.1 方向性

初期テーマは和風SFまたは和風サイバーパンク環境に合わせやすいものを想定するが、機能とテーマを密結合させない。

想定表現：

- 細い円弧、軌道、目盛り
- 選択項目の淡い発光
- 画面外に巨大な機構が続いているような表現
- 不透明な大きなパネルではなく、透過と余白を活かした構成

### 18.2 matugenを正式な色源とする

- 壁紙からの動的配色生成にはmatugenを使用する。
- matugenのテンプレート機能で、新ランチャー専用のJSONファイルを生成する。
- `Colors.qml`などQMLソースそのものをmatugenから上書きする方式は避ける。QML全体の再読み込みではなく、データファイルの再読込で色を更新する。
- QML側はQuickshellの`FileView`などで生成JSONを監視する。
- JSON更新を検知したら、テーマオブジェクトを更新し、すべてのUIへリアクティブに反映する。
- matugenの出力は途中状態を読まないよう、可能な限りアトミックに更新する。
- 必須キーが欠けている、JSONが壊れている、ファイルが存在しない場合は内蔵フォールバックテーマを使用する。
- ファイル破損時に全体をデフォルトへ戻すか、直前の有効テーマを維持するかは、直前テーマ維持を優先する。

matugen生成JSONの例：

```json
{
  "background": "#111318",
  "surface": "#191c22",
  "surfaceContainer": "#20242b",
  "primary": "#afc6ff",
  "secondary": "#c0c6dc",
  "tertiary": "#dfc0e8",
  "outline": "#8c9099",
  "text": "#e2e2e9",
  "error": "#ffb4ab"
}
```

実際のキー名はmatugenテンプレートとQML側のテーマモデルを同時に定義して確定する。

### 18.3 セマンティックテーマ

各QML部品がmatugenのパレット名を直接参照しないよう、アプリ内で意味ベースの色へ変換する。

例：

- `launcherBackground`
- `selectedItem`
- `normalItem`
- `inactiveItem`
- `primaryText`
- `secondaryText`
- `guideLine`
- `searchCursor`
- `errorColor`

これにより、matugenテーマ、固定テーマ、ユーザー定義テーマ、フォールバックテーマを同じUIへ適用できるようにする。

### 18.4 透明度

matugenはRGB色を提供し、透明度はランチャー側の設定として独立管理する。

初期設定項目の例：

```json
{
  "masterOpacity": 1.0,
  "backgroundOpacity": 0.72,
  "overlayOpacity": 0.12,
  "itemOpacity": 0.90,
  "inactiveOpacity": 0.46,
  "guideOpacity": 0.30,
  "textOpacity": 1.0,
  "imageOpacity": 1.0
}
```

透明度変更時は可能な限り即時反映し、設定値は再起動後も保持する。

### 18.5 色変更アニメーション

- matugenのテーマ更新時、色を即座に切り替えるだけでなく、QMLの`ColorAnimation`などで旧色から新色へ滑らかに遷移させる。
- 色遷移時間とイージングを設定可能にする。
- 壁紙選択直後、新しい壁紙の色へランチャーが染まってから閉じる演出を将来的に選択可能にする。
- アニメーション軽減設定が有効な場合は、色遷移を短縮または無効化する。

### 18.6 その他のカスタマイズ

少なくとも以下を変更可能にすることが望ましい。

- 背景色と透明度
- 通常色、選択色、無効色
- 文字色
- 円弧・ガイド線の色と太さ
- アイコン・画像の大きさ
- フォント
- 角丸、影、発光
- アニメーション速度

wofiのCSS互換は不要。

---

## 19. パフォーマンス目標

厳密な保証値ではなく、初期の目安として以下を目標とする。

- 温かい状態では、呼び出しから表示開始まで体感上ほぼ即時であること。
- 通常の文字・アイコンメニューは、おおむね100ミリ秒前後で表示開始できることを目標とする。
- 画像一覧は全サムネイル読み込みを待たずにUIを表示する。
- 大量項目でもスクロール中に目立つカクつきがないこと。
- デスクトップエントリ、アイコン、サムネイルは必要に応じてキャッシュする。

Quickshell/QML本体は常駐させ、CLIブリッジからIPCで表示要求を送る。これにより、Wayland接続、QMLコンポーネント、デスクトップエントリ、アイコン、テーマなどを再利用し、呼び出し遅延を抑える。

---

## 20. エラー処理

- 壊れた画像一件のためにメニュー全体を終了させない。
- 存在しないアイコンはプレースホルダーへフォールバックする。
- 設定ファイルの構文エラーは、ファイル名・行・原因を可能な限り表示する。
- 外部プロバイダーの失敗時は、エラーを表示して親階層へ戻れるようにする。
- 実行コマンドの失敗は標準エラーまたはログで確認できるようにする。
- dmenuモードの標準出力へログを混ぜない。
- 異常終了後もキーボードフォーカスや画面オーバーレイを残さない。

---

## 21. MVPの範囲

最初の実用品として、次を実装する。

### 21.1 必須

- Niri / Wayland上でのオーバーレイ表示
- 画面右下の扇形レイアウト
- 複数項目の表示
- 表示件数を超えた場合の円形スクロール
- 矢印キー、Enter、Escape操作
- マウスクリック、マウスホイール操作
- `/`または中央テキスト領域クリックによる検索モード
- プレーンテキストの`--dmenu`入力
- `--prompt`
- 選択した元の行を標準出力へ返す
- キャンセル時の空出力と終了コード
- `img:/absolute/path`入力のサムネイル表示
- PNG、JPEG、WebP対応
- 画像の非同期または段階的読み込み
- 最低限の設定ファイル
- 開閉、選択移動、円形スクロールのアニメーション
- 現行`select-wallpaper.sh`のwofi部分を置換できること
- Quickshell/QML本体とCLIブリッジの分離
- Quickshell `IpcHandler`などを使った開閉要求
- 設定可能な背景・項目・ガイド線などの透明度
- matugenテンプレートによる新ランチャー用テーマJSON生成
- テーマJSONの変更監視と再起動なしの色反映
- テーマ更新時の色トランジション
- テーマJSON破損時のフォールバック
- NixによるQuickshellバージョン固定と再現可能な実行環境

### 21.2 MVPに含めてもよい

- XDGデスクトップエントリの読み込み
- 基本的なユーザー定義階層
- 項目固有のキーバインド
- 外観プロファイル

ただし開発量が大きい場合は、まず壁紙スクリプトを置き換えられるdmenu機能を完成させ、その後に追加する。

### 21.3 MVPには含めない

- 動画の直接再生プレビュー
- 壁紙適用処理そのもの
- GUI設定エディタ
- マウス軌跡認識
- ストリーミング候補更新
- プラグインシステム
- X11対応
- wofi CSS互換
- すべてのwofi CLIオプション互換
- 背景ぼかし
- pywalを新ランチャーの色源として利用する機能

---

## 22. 推奨実装フェーズ

### Phase 0：技術検証

- 固定したQuickshellバージョンをNix環境へ導入する。
- Quickshell/QMLでNiri上に透明な`PanelWindow`を作り、右下へ表示する。
- キーボードフォーカスを取得する。
- 右下の極座標上にダミー項目を描画する。
- 画像一枚をサムネイル表示する。
- `IpcHandler`経由で外部コマンドから表示と非表示を切り替える。
- QMLで透明度を変更し、即時反映を確認する。
- ダミーのテーマJSONを書き換え、QMLを再起動せず色が変わることを確認する。

### Phase 1：壁紙選択MVP

- `--dmenu`
- `--prompt`
- 改行区切り入力
- `img:`解析
- 右下扇形表示
- 円形スクロール
- キーボード・マウス選択
- `/`検索
- stdoutへの元行返却
- `select-wallpaper.sh`での動作確認
- matugen用JSONテンプレート
- 壁紙適用後のmatugen実行
- テーマ変更監視と色アニメーション

### Phase 2：汎用ランチャー化

- XDGデスクトップエントリ
- ユーザー定義階層
- コマンド実行
- 項目キーバインド
- 設定ファイルの拡充

### Phase 3：動的メニューと高度な入力

- 外部プロバイダー
- JSON Lines
- 表示名、画像、返却値、検索語の分離
- ローディング・エラー状態

### Phase 4：完成度向上

- サムネイル・アイコンキャッシュ
- プロファイル
- 追加テーマプリセット
- 部位別透明度の拡充
- IPCの多重要求・異常終了耐性
- マルチモニター調整
- アニメーション最適化

---

## 23. MVP受け入れ条件

以下をすべて満たした時点で、壁紙選択MVPを完了とする。

1. Niri上でキーバインドからランチャーを呼び出せる。
2. ランチャーが画面右下から扇形に表示される。
3. `printf '%s\n' Image Video | radial-launcher --dmenu --prompt 'Select Mode'`が動作する。
4. 矢印キーまたは設定済みキーで候補を選択できる。
5. マウスクリックとホイールでも操作できる。
6. Enterで選択した元の行が標準出力へ返る。
7. Escapeで何も出力せずキャンセルできる。
8. `img:/absolute/path/to/file`を画像サムネイルとして表示できる。
9. 画面に収まらない候補を円形スクロールで閲覧できる。
10. `/`を押すか中央テキスト領域をクリックすると検索できる。
11. 検索解除後、元の候補一覧と位置へ戻れる。
12. 壊れた画像や存在しない画像が混じってもメニュー全体は動作する。
13. 現行`select-wallpaper.sh`のwofi呼び出しを本アプリへ置換し、静止画を選択・適用できる。
14. 同様にローカル動画またはYouTube動画のサムネイルを選択し、既存処理へ正しい`img:`行を返せる。
15. dmenuモードの標準出力にログや装飾文字が混入しない。
16. Quickshell/QML本体が常駐し、CLIブリッジからIPCでメニューを開ける。
17. 背景、非選択項目、円弧などの透明度を設定から変更できる。
18. matugenが新ランチャー用テーマJSONを生成できる。
19. テーマJSONを書き換えると、Quickshellを再起動せず表示色が更新される。
20. テーマ更新時に旧色から新色へ滑らかに遷移する。
21. テーマJSONが存在しない、または壊れていても、フォールバックテーマで起動できる。
22. 壁紙変更後、選択した静止画または動画サムネイルを元にmatugen配色が更新される。
23. 使用するQuickshellバージョンがNixで固定され、同じ環境を再現できる。

---

## 24. Codexが実装前に提示すべき内容

実装開始前に、Codexは以下を短く提示すること。

1. 固定するQuickshellバージョンとNix導入方法
2. `PanelWindow` / layer-shellで右下オーバーレイを実現する方法
3. CLIブリッジとQuickshell IPCの要求・応答方式
4. QMLコンポーネント、状態管理、入力モデル、テーマモデルの構成案
5. matugenテンプレートとQML側テーマJSONのスキーマ案
6. Phase 0とPhase 1の具体的な実装順序
7. 想定される技術的リスク

ただし、長い机上設計だけで終わらせず、Phase 0の最小プロトタイプを早期に動かしてNiri上の挙動を検証すること。

---

## 25. 未決事項

以下は実装前またはプロトタイプ確認後に決定する。

- 固定するQuickshellのバージョン
- 既定の表示件数（候補は5～8件）
- 扇の正確な角度と半径
- 仮想円中心を画面外へどの程度ずらすか
- 選択中画像の大きさと配置
- キーボード選択時にカーソルを動かすか、円全体を回すか
- 検索のデフォルト範囲
- QML設定と外部JSONの責任分担
- matugenテーマJSONの正確な色トークン名とスキーマ
- マルチモニターでの既定表示先

これらはハードコードせず、可能な範囲で後から設定可能にする。

---

## 26. Codexへ渡す開始指示の例

以下を本要件書と一緒にCodexへ渡す。

> この要件書に基づいて、Quickshellを実行基盤としたQML製ランチャーを実装してください。まず既存リポジトリ、NixOS/Niri環境、現在の`select-wallpaper.sh`と`apply-wallpaper.sh`を確認し、固定するQuickshellバージョン、Nix導入方法、`PanelWindow` / layer-shellによる右下オーバーレイ、CLIブリッジとIPCの要求・応答方式、QMLコンポーネント構成、matugenテーマJSONのスキーマ、Phase 0～1の作業計画を提示してください。その後、右下扇形オーバーレイ、`--dmenu`入力、`img:`画像表示、円形スクロール、検索、透明度設定、matugenテーマ監視の最小プロトタイプを実装し、ビルド・テスト・実行方法を整備してください。MVPの最優先目標は、添付の`select-wallpaper.sh`にあるwofi呼び出しを置き換え、壁紙サムネイルを選択して元の`img:`行を標準出力へ返し、壁紙変更後にmatugen生成色をQuickshellの再起動なしで反映することです。既存の壁紙適用、ffmpeg、yt-dlp、mpv処理は再実装しないでください。pywalを利用している既存UIは壊さず、新ランチャーの色源としてmatugenを追加してください。未決事項を独断で大規模に作り込まず、UI上の判断が必要な箇所は設定可能な仮値で実装してください。
